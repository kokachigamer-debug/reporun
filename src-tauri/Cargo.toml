[package]
name = "reporun"
version = "0.1.0"
description = "RepoRun — classify and run GitHub repos locally, reliably."
authors = ["RepoRun"]
edition = "2021"
rust-version = "1.77"

[lib]
name = "reporun_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
tauri = { version = "2.1", features = ["devtools"] }
tauri-plugin-shell = "2.0"
tauri-plugin-dialog = "2.0"
tauri-plugin-fs = "2.0"
tauri-plugin-os = "2.0"
tauri-plugin-process = "2.0"
tauri-plugin-updater = "2.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
walkdir = "2"
regex = "1"
wait-timeout = "0.2"
uuid = { version = "1", features = ["v4"] }
sysinfo = "0.32"
sha2 = "0.10"
hmac = "0.12"
base64 = "0.22"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
log = "0.4"
env_logger = "0.11"
thiserror = "1"

[features]
# Code signing / release toggles wired through here (Section 13) — currently no-op,
# ready to enable without rework.
default = []
sign-installers = []

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
